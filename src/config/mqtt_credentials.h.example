#pragma once

// MQTT Configuration Template
// Copy this file to mqtt_credentials.h and fill in your actual MQTT broker details
// mqtt_credentials.h is in .gitignore and will not be committed to git

#define MQTT_BROKER "your_mqtt_broker_ip"
#define MQTT_PORT 1883
#define MQTT_USERNAME ""  // Leave empty if no authentication
#define MQTT_PASSWORD ""  // Leave empty if no authentication
#define MQTT_CLIENT_ID "bambu-rfid-reader"
#define MQTT_TOPIC "bambu/rfid/uid"

